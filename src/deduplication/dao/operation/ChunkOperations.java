package deduplication.dao.operation;

import org.apache.cassandra.thrift.*;

public class ChunkOperations {
	
	public void insertRow(String key, String Adler32, String name, String index, String length) {
		//Clock clock = new Clock(System.nanoTime());
		long time = System.nanoTime();		
		
	}
	
}
